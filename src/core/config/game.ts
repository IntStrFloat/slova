/** Игровые числа движка (спека 03). Меняются здесь, не в логике. */
export const GAME = {
  diskLetters: { min: 3, max: 7 },
  coinsPerGridWord: 5,
  coinsPerBonusWord: 2,
  levelClearBonus: 20,
} as const;
